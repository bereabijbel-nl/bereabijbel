---
interface Props {
	title?: string;
	huidigeNav?: "home" | "over" | "contact" | "bijbel";
}

const { title = "BereaBijbel", huidigeNav = "" } = Astro.props;

// Alleen indexeerbaar als expliciet ingesteld via omgevingsvariabele.
// Standaard (niet ingesteld, demo, preview) = noindex.
const indexable = import.meta.env.PUBLIC_INDEXABLE === "true";
---

<html lang="nl">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		{!indexable && <meta name="robots" content="noindex,nofollow" />}
		<title>{title}</title>
		<style>
			@import url("https://fonts.googleapis.com/css2?family=Lora:ital,wght@0,400;0,500;0,600;0,700;1,400;1,500&display=swap");

			:root {
				--light: #ffffff;
				--lightgray: #e5e5e5;
				--gray: #b3b3b3;
				--darkgray: #333333;
				--dark: #111111;
				--secondary: #1a1a1a;
				--tertiary: #757575;
				--bg: var(--light);
				--bg-soft: #f5f5f5;
				--border: var(--lightgray);
				--tekst: var(--darkgray);
				--accent: var(--secondary);
				--accent-donker: var(--dark);
				--gewogen: #9c7a2e;
				--gewogen-soft: #f7f0dd;
				--body-font: "Lora", Georgia, "Iowan Old Style", "Palatino Linotype", Palatino, serif;
			}
			@media (prefers-color-scheme: dark) {
				:root:not([data-theme="light"]) {
					--light: #1b1a17;
					--lightgray: #34322b;
					--gray: #6b6558;
					--darkgray: #e4dfd1;
					--dark: #f5f2ea;
					--secondary: #e8e3d4;
					--tertiary: #a39c89;
					--bg-soft: #242219;
					--gewogen: #d9b25c;
					--gewogen-soft: #322a17;
				}
			}
			:root[data-theme="dark"] {
				--light: #1b1a17;
				--lightgray: #34322b;
				--gray: #6b6558;
				--darkgray: #e4dfd1;
				--dark: #f5f2ea;
				--secondary: #e8e3d4;
				--tertiary: #a39c89;
				--bg-soft: #242219;
				--gewogen: #d9b25c;
				--gewogen-soft: #322a17;
			}

			* { box-sizing: border-box; }
			html, body { margin: 0; padding: 0; }
			body {
				font-family: var(--body-font);
				background: var(--bg);
				color: var(--tekst);
				line-height: 1.7;
				-webkit-font-smoothing: antialiased;
			}
			:focus-visible { outline: 2px solid var(--accent); outline-offset: 2px; }
			::selection { background: var(--bg-soft); }
			a { color: var(--accent); }

			.site-header {
				text-align: center;
				padding: 2.2rem 1rem 0;
				margin-bottom: 2rem;
			}
			.page-title {
				margin: 0 0 0.3rem;
				font-size: 1.75rem;
				font-weight: 700;
			}
			.page-title a {
				color: var(--secondary);
				text-decoration: none;
			}
			.page-title a:hover { color: var(--accent-donker); }
			.site-subtitle {
				margin: 0 0 1rem;
				font-size: 1.05rem;
				color: var(--secondary);
			}
			.subtitel-spatie { display: inline; }
			.subtitel-nl { display: none; }
			@media (max-width: 480px) {
				.subtitel-spatie { display: none; }
				.subtitel-nl { display: block; }
			}
			.site-nav {
				font-size: 0.82rem;
				text-align: center;
				border-top: 1px solid var(--border);
				border-bottom: 1px solid var(--border);
				padding: 1rem 0;
				margin: 0.8rem 0 0;
			}
			.site-nav a {
				font-family: var(--body-font);
				font-size: inherit;
				text-transform: uppercase;
				letter-spacing: 0.02em;
				color: var(--secondary);
				text-decoration: none;
			}
			.site-nav a:hover { text-decoration: underline; }
			.site-nav a[aria-current="true"] { color: var(--tertiary); cursor: default; }
			.site-nav a[aria-current="true"]:hover { text-decoration: none; }
			.nav-sep { color: var(--gray); margin: 0 0.5rem; }

			main {
				max-width: 700px;
				margin: 0 auto;
				padding: 0 1.2rem 4.5rem;
			}

			.prose p { margin: 0 0 1.1rem; }
			.prose ul { margin: 0 0 1.1rem; padding-left: 1.3rem; }
			.prose li { margin-bottom: 0.4rem; }
			h2.sectie {
				font-size: 1.4rem;
				font-weight: 700;
				color: var(--dark);
				margin: 2.2rem 0 1rem;
			}
			h2.sectie:first-child { margin-top: 0; }

			footer.foot {
				text-align: center;
				margin-top: 3.5rem;
				padding-top: 1.2rem;
				border-top: 1px solid var(--border);
				font-family: sans-serif;
				font-size: 0.74rem;
				color: var(--tertiary);
			}
			footer.foot span { margin: 0 0.6rem; }

			.toast {
				position: fixed;
				bottom: 1.5rem;
				left: 50%;
				transform: translateX(-50%);
				background: var(--dark);
				color: var(--light);
				padding: 0.5rem 1rem;
				border-radius: 6px;
				font-family: sans-serif;
				font-size: 0.85rem;
				opacity: 0.95;
				z-index: 50;
			}
			.toast[hidden] { display: none; }
		</style>
		<style is:global>
			/* ---------- zoekbalk, hoofdstuknavigatie: gedeeld door home en hoofdstukpagina's ---------- */
			.zoekbalk {
				position: relative;
				max-width: 640px;
				margin: 1.4rem auto 0.4rem;
			}
			#search {
				display: block;
				width: 100%;
				font-family: var(--body-font);
				font-size: 0.95rem;
				line-height: 1.5;
				text-align: center;
				border: 1px solid var(--border);
				border-radius: 999px;
				padding: 0.6rem 0.8rem;
				color: var(--tekst);
				background: var(--bg);
			}
			#search::placeholder { color: var(--gray); }
			#search:focus { outline: none; border-color: var(--accent); }
			.search-error {
				max-width: 640px;
				margin: 0.4rem auto 0;
				color: #a33;
				font-size: 0.8rem;
				font-family: sans-serif;
				text-align: center;
			}
			.search-error[hidden] { display: none; }

			#hoofdstuktitel {
				text-align: center;
				font-size: 1.75rem;
				font-weight: 700;
				color: var(--tekst);
				margin: 2rem 0 0.4rem;
			}

			.hfdnav {
				display: flex;
				justify-content: center;
				align-items: center;
				gap: 0.6rem;
				flex-wrap: wrap;
				font-size: 0.85rem;
				padding: 1rem 0;
				margin-bottom: 1.1rem;
			}
			.hfdnav select {
				font-family: var(--body-font);
				font-size: 0.85rem;
				border: 1px solid var(--border);
				border-radius: 999px;
				padding: 0.3rem 1.6rem 0.3rem 0.8rem;
				background: var(--bg);
				color: var(--accent-donker);
				cursor: pointer;
			}
			.hfdnav select:focus { outline: none; border-color: var(--accent); }

			/* ---------- versregels en detailpaneel ---------- */
			#chapter-verses, #home-verses { font-size: 1.05rem; }
			.vers { position: relative; border-radius: 8px; line-height: 1.75; }
			.vers-hoofd {
				display: flex;
				gap: 0.8rem;
				padding: 0.5rem 0.8rem 0.5rem 0.4rem;
				border-radius: 8px;
				cursor: pointer;
			}
			.vers-hoofd:hover { background: var(--bg-soft); }
			.vers.open .vers-hoofd { border-radius: 8px 8px 0 0; background: var(--bg-soft); }
			.vnr {
				color: var(--gray);
				font-size: 0.75rem;
				min-width: 1.7rem;
				text-align: right;
				padding-top: 0.35rem;
				flex-shrink: 0;
				font-family: sans-serif;
			}
			.vtekst { flex: 1; }
			.vers-detail {
				padding: 1rem 1.2rem 1.2rem;
				font-size: 0.92rem;
				background: var(--bg-soft);
				border-radius: 0 0 8px 8px;
			}
			.placeholder-detail { font-style: italic; color: var(--tertiary); margin: 0 0 0.8rem; }

			.detail-row { display: flex; gap: 0.7rem; padding: 0.4rem 0 0.9rem; }
			.detail-label {
				flex-shrink: 0;
				width: 6.4rem;
				font-weight: 700;
				color: var(--accent-donker);
				font-family: sans-serif;
				font-size: 0.78rem;
				padding-top: 0.15rem;
			}
			.detail-body { flex: 1; }
			.detail-body .grondtekst { font-style: italic; }

			.segmenten {
				display: flex;
				flex-wrap: wrap;
				gap: 0.4rem 1.1rem;
			}
			.segment {
				display: flex;
				flex-direction: column;
				align-items: center;
				text-align: center;
				min-width: 3.2rem;
				max-width: 11rem;
				padding: 0.2rem 0.3rem;
				border-radius: 4px;
				cursor: pointer;
			}
			.segment:hover, .segment:focus { background: var(--lightgray); outline: none; }
			.seg-grondtekst {
				font-style: italic;
				color: var(--dark);
				font-size: 1.05rem;
				margin: 0.1rem 0;
			}
			.seg-strong-btn {
				font-family: sans-serif;
				font-size: 0.6rem;
				color: var(--tertiary);
				background: none;
				border: none;
				padding: 0;
				cursor: pointer;
			}
			.seg-strong-btn:hover, .seg-strong-btn:focus { color: var(--accent); text-decoration: underline; }

			.woord-uitleg {
				margin-top: 1rem;
				padding-top: 0.9rem;
				border-top: 1px solid var(--border);
				font-size: 0.88rem;
				text-align: left;
			}
			.woord-uitleg[hidden] { display: none; }
			.uitleg-intro { margin: 0 0 0.6rem; color: var(--tekst); }
			.uitleg-intro b { color: var(--accent-donker); }
			.field-label {
				font-family: sans-serif;
				font-size: 0.66rem;
				letter-spacing: 0.06em;
				text-transform: uppercase;
				color: var(--tertiary);
				margin: 0.7rem 0 0.3rem;
			}
			.vindplaatsen-list { margin: 0; padding: 0; list-style: none; display: flex; flex-direction: column; gap: 0.2rem; }
			.vp-item { padding: 0.3rem 0.4rem; border-radius: 4px; }
			.vp-item-hoofd { display: flex; gap: 0.6rem; font-size: 0.88rem; }
			.vp-ref { color: var(--tekst); font-weight: 600; min-width: 9rem; }
			.vp-vorm { font-style: italic; color: var(--dark); }
			.vindplaatsen-note { margin: 0.35rem 0 0; font-size: 0.8rem; color: var(--tertiary); }

			.meld-btn {
				flex-shrink: 0;
				align-self: flex-start;
				margin-top: 0.3rem;
				width: 1.6rem;
				height: 1.6rem;
				display: flex;
				align-items: center;
				justify-content: center;
				border: none;
				border-radius: 999px;
				background: none;
				color: var(--tertiary);
				cursor: pointer;
				opacity: 0;
				transition: opacity 0.15s, background 0.15s, color 0.15s;
			}
			.vers-hoofd:hover .meld-btn { opacity: 1; }
			.meld-btn:hover { background: var(--lightgray); color: var(--accent-donker); }

			@keyframes flits {
				0% { background: var(--lightgray); }
				100% { background: var(--bg-soft); }
			}
			.vers.flits .vers-hoofd { animation: flits 1.6s ease-out 1; }

			.cta-wrap { text-align: center; margin-top: 1.4rem; }
			.pill-btn {
				display: inline-block;
				font-family: var(--body-font);
				font-weight: 700;
				font-size: 0.98rem;
				background: var(--accent-donker);
				color: var(--bg);
				border: none;
				padding: 0.7rem 1.5rem;
				border-radius: 999px;
				cursor: pointer;
				text-decoration: none;
			}
			.pill-btn:hover { opacity: 0.88; }

			@media (prefers-reduced-motion: reduce) {
				* { transition: none !important; }
			}
		</style>
	</head>
	<body>
		<header class="site-header">
			<h2 class="page-title"><a href="/">BereaBijbel</a></h2>
			<p class="site-subtitle">Een betrouwbare vertaling,<span class="subtitel-spatie"> </span><br class="subtitel-nl" />in begrijpelijk Nederlands<br /><i>Volledig licentievrij</i></p>
			<nav class="site-nav">
				<a href="/" aria-current={huidigeNav === "home" ? "true" : "false"}>Home</a><span class="nav-sep">|</span>
				<a href="/bijbel" aria-current={huidigeNav === "bijbel" ? "true" : "false"}>Bijbel</a><span class="nav-sep">|</span>
				<a href="/over" aria-current={huidigeNav === "over" ? "true" : "false"}>Over</a><span class="nav-sep">|</span>
				<a href="/contact" aria-current={huidigeNav === "contact" ? "true" : "false"}>Contact</a>
			</nav>
		</header>

		<main>
			<slot />
		</main>

		<footer class="foot">
			<span>CC0 — data</span><span>MIT — code</span>
		</footer>

		<div id="toast" class="toast" hidden></div>

		<script>
			let toastTimer: ReturnType<typeof setTimeout>;
			function toonToast(tekst: string) {
				const el = document.getElementById("toast");
				if (!el) return;
				el.textContent = tekst;
				el.hidden = false;
				clearTimeout(toastTimer);
				toastTimer = setTimeout(() => { el.hidden = true; }, 2400);
			}
			(window as any).toonToast = toonToast;
		</script>
	</body>
</html>
